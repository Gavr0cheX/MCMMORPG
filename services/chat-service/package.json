{
  "name": "@mmorpg/chat-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "ioredis": "latest",
    "zod": "latest"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "tsx": "latest",
    "typescript": "^5.9.2"
  }
}
